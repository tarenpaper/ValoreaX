# Frontend environment (Vite). Copy to `.env`. Only VITE_* vars reach the client.
VITE_API_BASE_URL=http://localhost:5001/api/v1
VITE_SUPABASE_URL=
# Public publishable key only. NEVER place a service-role/secret key in a VITE_* variable.
VITE_SUPABASE_PUBLISHABLE_KEY=
